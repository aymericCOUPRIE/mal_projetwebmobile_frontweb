import React, {useEffect, useState} from "react";
import {isAdmin} from "../../utils/utils";

export default function Festivals (){


    //dans le return if  isAdmin  la page else la page organisateurs
    return(
        <>
        </>
    );
}